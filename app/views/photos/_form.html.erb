<%= simple_form_for [@location, @location.photos.build] do |f| %>
  <%= f.input :title, label: "TITLE" %>
  <%= f.input :photo, label: "Uploa photo" %>
  <%= f.button :submit, class: "button" %>
<% end %>
